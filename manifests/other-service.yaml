apiVersion: v1
kind: Namespace
metadata:
  name: echo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: other-echo-server
  namespace: echo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: other-echo-server
  template:
    metadata:
      labels:
        app: other-echo-server
    spec:
      containers:
        - name: echo-server
          image: jmalloc/echo-server
          ports:
            - name: http-port
              containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: other-echo-server
  namespace: echo
spec:
  ports:
    - name: http-port
      port: 80
      targetPort: http-port
      protocol: TCP
  selector:
    app: other-echo-server
---
apiVersion: x.getambassador.io/v3alpha1
kind: AmbassadorMapping
metadata:
  name: other-echo-server
  namespace: echo
spec:
  hostname: '*'
  prefix: /other-echo/
  service: other-echo-server

